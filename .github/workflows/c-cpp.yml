name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: check path
      uses: actions/checkout
      run: ls -la
    - name: test
      run: make test
